import { ProjectContainer } from './project-container.model';

describe('ProjectContainer', () => {
  it('should create an instance', () => {
    expect(new ProjectContainer()).toBeTruthy();
  });
});
